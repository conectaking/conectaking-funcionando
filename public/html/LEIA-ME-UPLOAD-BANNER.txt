UPLOAD BANNER / CORS - CORREÃ‡ÃƒO
================================

O dashboard que usa "Editar Banner" (dashboard.html + dashboard.js) estÃ¡ a dar
"Failed to fetch" porque as chamadas Ã  API vÃ£o para conectaking.com.br (que
redireciona e nÃ£o devolve CORS).

SOLUÃ‡ÃƒO: No ficheiro dashboard.html do projeto onde estÃ¡ o painel (o que
abre em http://127.0.0.1:5500/public/html/dashboard.html), adicione no <head>,
ANTES de qualquer outro <script>, esta linha:

  <script src="https://www.conectaking.com.br/api-config.js"></script>

Assim, todos os fetch('/api/...') passam a ir para a API no Render e o upload
do banner deixa de falhar.

Se o seu dashboard.html estiver noutra pasta (por exemplo noutro repositÃ³rio),
adicione a mesma linha nesse dashboard.html.
