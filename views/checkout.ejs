<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pagamento - <%= formTitle %> | KingForms</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    <style>
        :root { --primary: #22c55e; --primary-dark: #16a34a; --bg: #f8fafc; --card: #fff; --text: #1e293b; --text-muted: #64748b; }
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { font-family: 'Inter', sans-serif; background: var(--bg); color: var(--text); min-height: 100vh; display: flex; flex-direction: column; }
        .top-bar { background: var(--card); padding: 16px 24px; box-shadow: 0 1px 3px rgba(0,0,0,.08); }
        .top-bar .logo { font-weight: 700; font-size: 1.25rem; color: var(--primary); }
        .main { flex: 1; padding: 32px 24px; max-width: 900px; margin: 0 auto; width: 100%; }
        .card { background: var(--card); border-radius: 12px; box-shadow: 0 4px 12px rgba(0,0,0,.06); overflow: hidden; display: grid; grid-template-columns: 1fr 1fr; gap: 0; }
        @media (max-width: 700px) { .card { grid-template-columns: 1fr; } }
        .col { padding: 28px; }
        .col-left { border-right: 1px solid #e2e8f0; }
        @media (max-width: 700px) { .col-left { border-right: none; border-bottom: 1px solid #e2e8f0; } }
        .col h2 { font-size: 1.125rem; color: var(--text-muted); margin-bottom: 8px; font-weight: 600; }
        .col .form-title { font-size: 1.25rem; color: var(--text); margin-bottom: 16px; }
        .price { font-size: 1.75rem; font-weight: 700; color: var(--primary); }
        .status-badge { display: inline-block; padding: 6px 12px; border-radius: 8px; font-size: 0.875rem; font-weight: 600; margin-top: 12px; }
        .status-pending { background: #fef3c7; color: #92400e; }
        .status-paid { background: #d1fae5; color: #065f46; }
        .btn { display: inline-flex; align-items: center; justify-content: center; gap: 8px; padding: 14px 24px; border-radius: 10px; font-weight: 600; font-size: 1rem; border: none; cursor: pointer; transition: background .2s; text-decoration: none; }
        .btn-primary { background: var(--primary); color: #fff; }
        .btn-primary:hover { background: var(--primary-dark); }
        .btn-secondary { background: #e2e8f0; color: var(--text); }
        .btn-secondary:hover { background: #cbd5e1; }
        .method-card { border: 2px solid #e2e8f0; border-radius: 10px; padding: 20px; margin-bottom: 16px; cursor: pointer; transition: border-color .2s; }
        .method-card:hover, .method-card.selected { border-color: var(--primary); background: #f0fdf4; }
        .method-card h3 { font-size: 1rem; margin-bottom: 4px; }
        .method-card p { font-size: 0.875rem; color: var(--text-muted); }
        .qrcode-wrap { text-align: center; padding: 20px; background: #f8fafc; border-radius: 10px; margin: 16px 0; }
        .qrcode-wrap img { max-width: 220px; height: auto; }
        .copy-pix { margin-top: 12px; font-size: 0.75rem; color: var(--text-muted); word-break: break-all; }
        .footer { padding: 24px; text-align: center; color: var(--text-muted); font-size: 0.875rem; }
        .footer a { color: var(--primary); text-decoration: none; }
        .loading { text-align: center; padding: 40px; color: var(--text-muted); }
        .err-msg { background: #fee2e2; color: #991b1b; padding: 12px; border-radius: 8px; margin-bottom: 16px; font-size: 0.875rem; }
        .link-pagbank { font-size: 0.875rem; margin-top: 16px; }
        .link-pagbank a { color: var(--primary); }
        .card-form { display: none; margin-top: 16px; }
        .card-form.visible { display: block; }
        .card-form label { display: block; margin-bottom: 4px; font-size: 0.875rem; font-weight: 600; color: var(--text); }
        .card-form input, .card-form select { width: 100%; padding: 12px; border-radius: 8px; border: 1px solid #e2e8f0; margin-bottom: 12px; font-size: 1rem; }
        .card-form .row { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 12px; }
        .card-form .row-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }
        .card-form .btn-block { width: 100%; margin-top: 8px; }
    </style>
</head>
<body>
    <header class="top-bar">
        <div class="logo">KingForms</div>
    </header>
    <main class="main">
        <div class="card">
            <div class="col col-left">
                <h2>Resumo</h2>
                <p class="form-title"><%= formTitle %></p>
                <p class="price" id="price-display">R$ <%= (priceCents / 100).toFixed(2).replace('.', ',') %></p>
                <% if (responderName) { %><p style="margin-top:12px;color:var(--text-muted);"><%= responderName %></p><% } %>
                <span class="status-badge status-<%= paymentStatus === 'PAID' ? 'paid' : 'pending' %>" id="status-badge">
                    <%= paymentStatus === 'PAID' ? 'Pagamento confirmado' : 'Pagamento pendente' %>
                </span>
            </div>
            <div class="col">
                <h2>Método de pagamento</h2>
                <div id="state-loading" class="loading" style="display:none;">Carregando...</div>
                <div id="state-error" class="err-msg" style="display:none;"></div>
                <div id="state-pending">
                    <div class="method-card selected" data-method="pix">
                        <h3><i class="fas fa-qrcode"></i> Pix (instantâneo)</h3>
                        <p>QR Code ou código copia e cola</p>
                    </div>
                    <div class="method-card" data-method="card">
                        <h3><i class="fas fa-credit-card"></i> Cartão de crédito ou débito</h3>
                        <p>Crédito em até 12x ou débito à vista</p>
                    </div>
                    <div id="pix-section">
                        <div id="qrcode-container" style="display:none;">
                            <div class="qrcode-wrap">
                                <img id="qrcode-img" alt="Pix QR Code" />
                                <p class="copy-pix" id="pix-copy-text"></p>
                                <button type="button" class="btn btn-secondary" id="btn-copy-pix" style="margin-top:8px;">Copiar código Pix</button>
                            </div>
                        </div>
                        <button type="button" class="btn btn-primary" id="btn-generate-pix" style="margin-top:16px;width:100%;">
                            Gerar QR Code Pix
                        </button>
                    </div>
                    <div id="card-section" class="card-form">
                        <label>Número do cartão</label>
                        <input type="text" id="card-number" placeholder="0000 0000 0000 0000" maxlength="19" autocomplete="cc-number" />
                        <div class="row">
                            <div>
                                <label>Validade (mês)</label>
                                <input type="text" id="card-exp-month" placeholder="MM" maxlength="2" autocomplete="cc-exp-month" />
                            </div>
                            <div>
                                <label>Ano</label>
                                <input type="text" id="card-exp-year" placeholder="AA" maxlength="4" autocomplete="cc-exp-year" />
                            </div>
                            <div>
                                <label>CVV</label>
                                <input type="text" id="card-cvv" placeholder="123" maxlength="4" autocomplete="cc-csc" />
                            </div>
                        </div>
                        <label>Nome no cartão</label>
                        <input type="text" id="card-holder" placeholder="Como está no cartão" autocomplete="cc-name" />
                        <label>CPF do titular</label>
                        <input type="text" id="card-holder-cpf" placeholder="Apenas números" maxlength="14" />
                        <label id="installments-label">Parcelas (crédito)</label>
                        <select id="card-installments">
                            <option value="1">1x sem juros</option>
                            <option value="2">2x sem juros</option>
                            <option value="3">3x sem juros</option>
                            <option value="4">4x sem juros</option>
                            <option value="5">5x sem juros</option>
                            <option value="6">6x sem juros</option>
                            <option value="7">7x sem juros</option>
                            <option value="8">8x sem juros</option>
                            <option value="9">9x sem juros</option>
                            <option value="10">10x sem juros</option>
                            <option value="11">11x sem juros</option>
                            <option value="12">12x sem juros</option>
                        </select>
                        <button type="button" class="btn btn-primary btn-block" id="btn-pay-credit"><i class="fas fa-credit-card"></i> Pagar com crédito</button>
                        <button type="button" class="btn btn-secondary btn-block" id="btn-pay-debit"><i class="fas fa-university"></i> Pagar com débito</button>
                    </div>
                </div>
                <div id="state-paid" style="display:none;">
                    <p style="margin-bottom:16px;">Pagamento confirmado. Obrigado!</p>
                    <a href="<%= baseUrl %>/<%= slug %>/form/<%= itemId %>/success?response_id=<%= submissionId %>&show_success_page=true" class="btn btn-primary">Continuar</a>
                </div>
                <p class="link-pagbank">Não tem conta no PagBank? <a href="https://www.pagbank.com.br" target="_blank" rel="noopener">Crie gratuitamente</a></p>
            </div>
        </div>
    </main>
    <footer class="footer">
        KingForms by ConectaKing
    </footer>
    <script>
        (function() {
            const submissionId = '<%= submissionId %>';
            const baseUrl = '<%= baseUrl %>';
            const slug = '<%= slug %>';
            const itemId = '<%= itemId %>';
            const paymentStatus = '<%= paymentStatus %>';

            if (paymentStatus === 'PAID') {
                document.getElementById('state-pending').style.display = 'none';
                document.getElementById('state-paid').style.display = 'block';
                return;
            }

            const btnGenerate = document.getElementById('btn-generate-pix');
            const qrcodeContainer = document.getElementById('qrcode-container');
            const qrcodeImg = document.getElementById('qrcode-img');
            const pixCopyText = document.getElementById('pix-copy-text');
            const btnCopyPix = document.getElementById('btn-copy-pix');
            const stateLoading = document.getElementById('state-loading');
            const stateError = document.getElementById('state-error');

            let pixCodeText = '';

            btnGenerate.addEventListener('click', async function() {
                stateError.style.display = 'none';
                stateLoading.style.display = 'block';
                btnGenerate.disabled = true;
                try {
                    const res = await fetch(baseUrl + '/api/checkout/create', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ submissionId: submissionId, method: 'pix' })
                    });
                    const data = await res.json();
                    stateLoading.style.display = 'none';
                    if (!res.ok) {
                        stateError.textContent = data.error || 'Erro ao gerar cobrança.';
                        stateError.style.display = 'block';
                        btnGenerate.disabled = false;
                        return;
                    }
                    if (data.qrCode) {
                        qrcodeImg.src = data.qrCode.startsWith('data:') ? data.qrCode : 'data:image/png;base64,' + data.qrCode;
                        qrcodeImg.style.display = 'block';
                    }
                    if (data.qrCodeText) {
                        pixCodeText = data.qrCodeText;
                        pixCopyText.textContent = pixCodeText.substring(0, 60) + '...';
                    }
                    qrcodeContainer.style.display = 'block';
                    btnGenerate.style.display = 'none';
                } catch (e) {
                    stateLoading.style.display = 'none';
                    stateError.textContent = 'Erro de conexão. Tente novamente.';
                    stateError.style.display = 'block';
                    btnGenerate.disabled = false;
                }
            });

            btnCopyPix.addEventListener('click', function() {
                if (!pixCodeText) return;
                navigator.clipboard.writeText(pixCodeText).then(function() {
                    const t = btnCopyPix.textContent;
                    btnCopyPix.textContent = 'Copiado!';
                    setTimeout(function() { btnCopyPix.textContent = t; }, 2000);
                });
            });

            // Alternar entre Pix e Cartão
            const methodCards = document.querySelectorAll('.method-card');
            const pixSection = document.getElementById('pix-section');
            const cardSection = document.getElementById('card-section');
            function selectMethod(method) {
                methodCards.forEach(function(el) {
                    el.classList.toggle('selected', el.getAttribute('data-method') === method);
                });
                pixSection.style.display = method === 'pix' ? 'block' : 'none';
                cardSection.classList.toggle('visible', method === 'card');
            }
            methodCards.forEach(function(el) {
                el.addEventListener('click', function() {
                    selectMethod(el.getAttribute('data-method'));
                });
            });

            // Máscara número do cartão (espaços)
            document.getElementById('card-number').addEventListener('input', function() {
                let v = this.value.replace(/\D/g, '');
                this.value = v.replace(/(\d{4})(?=\d)/g, '$1 ').trim();
            });
            document.getElementById('card-holder-cpf').addEventListener('input', function() {
                this.value = this.value.replace(/\D/g, '').slice(0, 11);
            });

            function getCardPayload() {
                const number = (document.getElementById('card-number').value || '').replace(/\D/g, '');
                const expMonth = (document.getElementById('card-exp-month').value || '').replace(/\D/g, '').slice(0, 2);
                const yearRaw = (document.getElementById('card-exp-year').value || '').replace(/\D/g, '');
                const expYearShort = yearRaw.length >= 4 ? yearRaw.slice(-2) : yearRaw.slice(-2);
                return {
                    number: number,
                    exp_month: expMonth,
                    exp_year: expYearShort,
                    security_code: (document.getElementById('card-cvv').value || '').replace(/\D/g, '').slice(0, 4),
                    holder_name: (document.getElementById('card-holder').value || '').trim(),
                    holder_tax_id: (document.getElementById('card-holder-cpf').value || '').replace(/\D/g, '').slice(0, 11),
                    installments: parseInt(document.getElementById('card-installments').value, 10) || 1
                };
            }

            function payWithCard(method) {
                const card = getCardPayload();
                if (!card.number || card.number.length < 13) {
                    stateError.textContent = 'Informe o número do cartão.';
                    stateError.style.display = 'block';
                    return;
                }
                if (!card.exp_month || !card.exp_year) {
                    stateError.textContent = 'Informe validade (mês e ano).';
                    stateError.style.display = 'block';
                    return;
                }
                if (!card.security_code || card.security_code.length < 3) {
                    stateError.textContent = 'Informe o CVV.';
                    stateError.style.display = 'block';
                    return;
                }
                if (!card.holder_name) {
                    stateError.textContent = 'Informe o nome do titular.';
                    stateError.style.display = 'block';
                    return;
                }
                stateError.style.display = 'none';
                stateLoading.style.display = 'block';
                document.getElementById('btn-pay-credit').disabled = true;
                document.getElementById('btn-pay-debit').disabled = true;
                fetch(baseUrl + '/api/checkout/create', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        submissionId: submissionId,
                        method: method,
                        card_number: card.number,
                        exp_month: card.exp_month,
                        exp_year: card.exp_year,
                        security_code: card.security_code,
                        holder_name: card.holder_name,
                        holder_tax_id: card.holder_tax_id || undefined,
                        installments: method === 'credit_card' ? card.installments : 1
                    })
                })
                .then(function(r) { return r.json().then(function(d) { return { ok: r.ok, data: d }; }); })
                .then(function(result) {
                    stateLoading.style.display = 'none';
                    document.getElementById('btn-pay-credit').disabled = false;
                    document.getElementById('btn-pay-debit').disabled = false;
                    if (!result.ok) {
                        stateError.textContent = result.data.error || 'Erro ao processar pagamento.';
                        stateError.style.display = 'block';
                        return;
                    }
                    if (result.data.paid) {
                        document.getElementById('state-pending').style.display = 'none';
                        document.getElementById('state-paid').style.display = 'block';
                    } else {
                        stateError.textContent = 'Pagamento em análise ou não aprovado. Tente outro cartão ou use Pix.';
                        stateError.style.display = 'block';
                    }
                })
                .catch(function(e) {
                    stateLoading.style.display = 'none';
                    document.getElementById('btn-pay-credit').disabled = false;
                    document.getElementById('btn-pay-debit').disabled = false;
                    stateError.textContent = 'Erro de conexão. Tente novamente.';
                    stateError.style.display = 'block';
                });
            }
            document.getElementById('btn-pay-credit').addEventListener('click', function() { payWithCard('credit_card'); });
            document.getElementById('btn-pay-debit').addEventListener('click', function() { payWithCard('debit_card'); });
        })();
    </script>
</body>
</html>
