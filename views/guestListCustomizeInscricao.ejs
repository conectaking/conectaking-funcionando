<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Personalizar Página de Inscrição - <%= formData.form_title || 'Formulário' %></title>
    <link rel="icon" type="image/png" href="<%= typeof faviconUrl !== 'undefined' ? faviconUrl : 'https://i.ibb.co/60sW9k75/logo.png' %>">
    <link rel="apple-touch-icon" href="<%= typeof faviconUrl !== 'undefined' ? faviconUrl : 'https://i.ibb.co/60sW9k75/logo.png' %>">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #0D0D0F 0%, #1C1C21 100%);
            min-height: 100vh;
            padding: 20px;
            color: #ECECEC;
        }
        
        .container {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 400px 1fr;
            gap: 24px;
        }
        
        .sidebar {
            background: rgba(255,255,255,0.05);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255,255,255,0.1);
            border-radius: 20px;
            padding: 24px;
            height: fit-content;
            position: sticky;
            top: 20px;
        }
        
        .preview-area {
            background: rgba(255,255,255,0.03);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255,255,255,0.1);
            border-radius: 20px;
            padding: 24px;
            min-height: 600px;
        }
        
        .section-title {
            font-size: 18px;
            font-weight: 700;
            color: #FFC700;
            margin-bottom: 16px;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #ECECEC;
            font-weight: 600;
            font-size: 14px;
        }
        
        .form-group input[type="color"],
        .form-group input[type="text"],
        .form-group input[type="url"],
        .form-group input[type="range"] {
            width: 100%;
            padding: 12px;
            background: rgba(0,0,0,0.3);
            border: 2px solid rgba(255,255,255,0.1);
            border-radius: 12px;
            color: #ECECEC;
            font-size: 14px;
            transition: all 0.3s;
        }
        
        .form-group input:focus {
            outline: none;
            border-color: #FFC700;
            background: rgba(0,0,0,0.4);
        }
        
        .color-input-wrapper {
            display: flex;
            gap: 12px;
            align-items: center;
        }
        
        .color-input-wrapper input[type="color"] {
            width: 60px;
            height: 50px;
            border-radius: 8px;
            cursor: pointer;
            border: 2px solid rgba(255,255,255,0.2);
        }
        
        .color-input-wrapper input[type="text"] {
            flex: 1;
        }
        
        .image-upload-area {
            border: 2px dashed rgba(255,255,255,0.2);
            border-radius: 12px;
            padding: 20px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s;
            background: rgba(0,0,0,0.2);
        }
        
        .image-upload-area:hover {
            border-color: #FFC700;
            background: rgba(255,199,0,0.05);
        }
        
        .image-upload-area.has-image {
            border-style: solid;
            padding: 0;
        }
        
        .image-preview {
            width: 100%;
            max-height: 200px;
            object-fit: cover;
            border-radius: 12px;
            display: block;
        }
        
        .btn-save {
            width: 100%;
            padding: 16px;
            background: linear-gradient(135deg, #FFC700, #FFA500);
            color: #000;
            border: none;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s;
            margin-top: 24px;
        }
        
        .btn-save:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 24px rgba(255, 199, 0, 0.4);
        }
        
        .preview-frame {
            width: 100%;
            height: 800px;
            border: 2px solid rgba(255,255,255,0.1);
            border-radius: 12px;
            overflow: hidden;
            background: white;
        }
        
        .preview-frame iframe {
            width: 100%;
            height: 100%;
            border: none;
        }
        
        @media (max-width: 1024px) {
            .container {
                grid-template-columns: 1fr;
            }
            
            .sidebar {
                position: static;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="sidebar">
            <h1 style="color: #FFC700; margin-bottom: 24px; font-size: 24px;">
                <i class="fas fa-edit"></i> Personalizar Página de Inscrição
            </h1>
            
            <form id="customize-form">
                <!-- Cores -->
                <div class="form-group">
                    <label class="section-title">
                        <i class="fas fa-fill-drip"></i> Cores
                    </label>
                    
                    <label>Cor Primária (Gradiente)</label>
                    <div class="color-input-wrapper">
                        <input type="color" id="primary_color" value="<%= formData.primary_color || '#4A90E2' %>" onchange="updateColor('primary_color', this.value)">
                        <input type="text" id="primary_color_text" value="<%= formData.primary_color || '#4A90E2' %>" onchange="updateColorFromText('primary_color', this.value)">
                    </div>
                    
                    <label style="margin-top: 12px;">Cor Secundária (Gradiente)</label>
                    <div class="color-input-wrapper">
                        <input type="color" id="secondary_color" value="<%= formData.secondary_color || '#6BA3F0' %>" onchange="updateColor('secondary_color', this.value)">
                        <input type="text" id="secondary_color_text" value="<%= formData.secondary_color || '#6BA3F0' %>" onchange="updateColorFromText('secondary_color', this.value)">
                    </div>
                    
                    <label style="margin-top: 12px;">Cor de Fundo</label>
                    <div class="color-input-wrapper">
                        <input type="color" id="background_color" value="<%= formData.background_color || '#FFFFFF' %>" onchange="updateColor('background_color', this.value)">
                        <input type="text" id="background_color_text" value="<%= formData.background_color || '#FFFFFF' %>" onchange="updateColorFromText('background_color', this.value)">
                    </div>
                </div>
                
                <!-- Imagens -->
                <div class="form-group">
                    <label class="section-title">
                        <i class="fas fa-images"></i> Imagens
                    </label>
                    
                    <label>Imagem de Fundo</label>
                    <div class="image-upload-area <%= formData.background_image_url ? 'has-image' : '' %>" onclick="document.getElementById('background_image_input').click()">
                        <% if (formData.background_image_url) { %>
                            <img src="<%= formData.background_image_url %>" class="image-preview" id="background_image_preview">
                        <% } else { %>
                            <i class="fas fa-image" style="font-size: 48px; color: #A1A1A1; margin-bottom: 12px;"></i>
                            <p style="color: #A1A1A1;">Clique para adicionar fundo</p>
                        <% } %>
                        <input type="file" id="background_image_input" accept="image/*" style="display: none;" onchange="handleImageUpload('background_image', this)">
                    </div>
                    <input type="hidden" id="background_image_url" name="background_image_url" value="<%= formData.background_image_url || '' %>">
                    
                    <label style="margin-top: 16px;">Opacidade do Fundo</label>
                    <input type="range" id="background_opacity" name="background_opacity" min="0" max="1" step="0.1" value="<%= formData.background_opacity || 1.0 %>" oninput="document.getElementById('opacity_value').textContent = this.value">
                    <div style="text-align: center; color: #A1A1A1; font-size: 12px; margin-top: 4px;">
                        Opacidade: <span id="opacity_value"><%= formData.background_opacity || 1.0 %></span>
                    </div>
                </div>
                
                <button type="submit" class="btn-save">
                    <i class="fas fa-save"></i> Salvar Personalização
                </button>
            </form>
        </div>
        
        <div class="preview-area">
            <h2 style="color: #FFC700; margin-bottom: 16px;">
                <i class="fas fa-eye"></i> Preview
            </h2>
            <div class="preview-frame">
                <iframe id="preview-iframe" src="/<%= profileSlug || 'preview' %>/form/<%= profileItemId %>/success?response_id=preview"></iframe>
            </div>
        </div>
    </div>
    
    <script>
        const profileItemId = <%= profileItemId %>;
        
        function updateColor(field, value) {
            document.getElementById(`${field}_text`).value = value;
        }
        
        function updateColorFromText(field, value) {
            if (/^#[0-9A-F]{6}$/i.test(value)) {
                document.getElementById(field).value = value;
            }
        }
        
        async function handleImageUpload(type, input) {
            const file = input.files[0];
            if (!file) return;
            
            const formData = new FormData();
            formData.append('image', file);
            
            try {
                const response = await fetch('/api/upload/image', {
                    method: 'POST',
                    body: formData
                });
                
                const data = await response.json();
                if (data.url) {
                    document.getElementById('background_image_url').value = data.url;
                    const preview = document.getElementById('background_image_preview');
                    if (preview) {
                        preview.src = data.url;
                        preview.parentElement.classList.add('has-image');
                    } else {
                        const uploadArea = document.querySelector('.image-upload-area');
                        uploadArea.innerHTML = `<img src="${data.url}" class="image-preview" id="background_image_preview">`;
                        uploadArea.classList.add('has-image');
                    }
                }
            } catch (error) {
                console.error('Erro ao fazer upload:', error);
                alert('Erro ao fazer upload da imagem');
            }
        }
        
        document.getElementById('customize-form').addEventListener('submit', async (e) => {
            e.preventDefault();
            
            const formData = {
                primary_color: document.getElementById('primary_color').value,
                secondary_color: document.getElementById('secondary_color').value,
                background_color: document.getElementById('background_color').value,
                background_image_url: document.getElementById('background_image_url').value,
                background_opacity: document.getElementById('background_opacity').value
            };
            
            try {
                const response = await fetch(`/api/guest-lists/${profileItemId}/customize-inscricao`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(formData)
                });
                
                const data = await response.json();
                if (data.success) {
                    alert('Personalização salva com sucesso!');
                    // Recarregar preview
                    document.getElementById('preview-iframe').src = document.getElementById('preview-iframe').src;
                } else {
                    alert('Erro ao salvar: ' + (data.message || 'Erro desconhecido'));
                }
            } catch (error) {
                console.error('Erro:', error);
                alert('Erro ao salvar personalização');
            }
        });
    </script>
</body>
</html>
