<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Assinar Contrato: <%= contract.title %></title>
    <link rel="icon" type="image/png" href="<%= typeof faviconUrl !== 'undefined' ? faviconUrl : 'https://i.ibb.co/60sW9k75/logo.png' %>">
    <link rel="apple-touch-icon" href="<%= typeof faviconUrl !== 'undefined' ? faviconUrl : 'https://i.ibb.co/60sW9k75/logo.png' %>">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/signature_pad@4.0.9/dist/signature_pad.umd.min.js"></script>
    <!-- PDF.js para preview de PDFs -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.min.js"></script>
    <script>
        // Garantir que pdfjsLib está disponível globalmente
        if (typeof pdfjsLib !== 'undefined') {
            pdfjsLib.GlobalWorkerOptions.workerSrc = 'https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js';
        }
    </script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #991B1B 0%, #000000 100%);
            min-height: 100vh;
            padding: 20px;
            color: #FFFFFF;
        }
        
        .container {
            max-width: 900px;
            margin: 0 auto;
            background: rgba(28, 28, 33, 0.95);
            border-radius: 16px;
            padding: 40px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
        }
        
        .header {
            text-align: center;
            margin-bottom: 40px;
        }
        
        .header h1 {
            font-size: 2rem;
            margin-bottom: 10px;
            color: #FFC700;
        }
        
        .header p {
            color: #888888;
            font-size: 1rem;
        }
        
        .contract-preview {
            background: linear-gradient(135deg, #1C1C21 0%, #2C2C2F 100%);
            border: 2px solid #FFC700;
            border-radius: 16px;
            padding: 30px;
            margin-bottom: 40px;
            max-height: 700px;
            overflow-y: auto;
            position: relative;
            box-shadow: 0 8px 32px rgba(255, 199, 0, 0.1);
        }
        
        .contract-preview::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #FFC700, #FFD700, #FFC700);
            border-radius: 16px 16px 0 0;
            animation: shimmer 2s infinite;
        }
        
        @keyframes shimmer {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.8; }
        }
        
        .contract-preview h2 {
            font-size: 1.5rem;
            margin-bottom: 20px;
            color: #FFC700;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .contract-content {
            white-space: pre-wrap;
            line-height: 2;
            color: #ECECEC;
            font-size: 1rem;
            background: rgba(0, 0, 0, 0.3);
            padding: 25px;
            border-radius: 12px;
            border-left: 4px solid #FFC700;
        }
        
        /* Scrollbar personalizada */
        .contract-preview::-webkit-scrollbar {
            width: 10px;
        }
        
        .contract-preview::-webkit-scrollbar-track {
            background: #1C1C21;
            border-radius: 10px;
        }
        
        .contract-preview::-webkit-scrollbar-thumb {
            background: linear-gradient(135deg, #FFC700, #F59E0B);
            border-radius: 10px;
        }
        
        .contract-preview::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(135deg, #FFD700, #FFC700);
        }
        
        .signature-section {
            background: #1C1C21;
            border: 2px solid #2C2C2F;
            border-radius: 12px;
            padding: 30px;
            margin-bottom: 30px;
        }
        
        .signature-section h3 {
            font-size: 1.3rem;
            margin-bottom: 20px;
            color: #FFC700;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .signature-section h3::before {
            content: '';
            width: 4px;
            height: 30px;
            background: #FFC700;
            border-radius: 2px;
        }
        
        .signature-methods {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }
        
        .signature-method {
            background: #0B0B0B;
            border: 2px solid #2C2C2F;
            border-radius: 8px;
            padding: 20px;
        }
        
            .signature-method.active {
            border-color: #FFC700;
            background: rgba(255, 199, 0, 0.05);
        }
        
        .signature-method:hover {
            border-color: #3C3C3F;
            cursor: pointer;
        }
        
        .signature-method.active:hover {
            border-color: #FFC700;
        }
        
        .signature-method h4 {
            font-size: 1.1rem;
            margin-bottom: 15px;
            color: #FFFFFF;
        }
        
        .signature-canvas-container {
            border: 2px dashed #2C2C2F;
            border-radius: 8px;
            padding: 15px;
            background: #FFFFFF;
            margin-bottom: 15px;
        }
        
        .signature-canvas {
            width: 100%;
            height: 200px;
            border: none;
            cursor: crosshair;
        }
        
        .canvas-controls {
            display: flex;
            gap: 10px;
            margin-top: 10px;
        }
        
        .signature-upload-container {
            margin-top: 10px;
        }
        
        .signature-typed-input {
            width: 100%;
            padding: 15px;
            font-size: 1.2rem;
            border: 2px solid #2C2C2F;
            border-radius: 8px;
            background: #0B0B0B;
            color: #FFFFFF;
            font-family: 'Inter', sans-serif;
            text-align: center;
        }
        
        .btn {
            padding: 12px 24px;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }
        
        .btn-primary {
            background: #FFC700;
            color: #000000;
        }
        
        .btn-primary:hover {
            background: #FFD700;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(255, 199, 0, 0.3);
        }
        
        .btn-secondary {
            background: #2C2C2F;
            color: #FFFFFF;
        }
        
        .btn-secondary:hover {
            background: #3C3C3F;
        }
        
        .btn-danger {
            background: #991B1B;
            color: #FFFFFF;
        }
        
        .btn-danger:hover {
            background: #B91C1C;
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #ECECEC;
            font-weight: 600;
        }
        
        .form-control {
            width: 100%;
            padding: 12px;
            border: 2px solid #2C2C2F;
            border-radius: 8px;
            background: #0B0B0B;
            color: #FFFFFF;
            font-size: 1rem;
            font-family: 'Inter', sans-serif;
        }
        
        .checkbox-group {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-top: 20px;
        }
        
        .checkbox-group input[type="checkbox"] {
            width: 20px;
            height: 20px;
            cursor: pointer;
        }
        
        .checkbox-group label {
            color: #ECECEC;
            cursor: pointer;
            margin: 0;
        }
        
        .actions {
            display: flex;
            gap: 15px;
            justify-content: flex-end;
            margin-top: 30px;
        }
        
        .alert {
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
        }
        
        .alert-success {
            background: rgba(34, 197, 94, 0.2);
            border: 1px solid #22C55E;
            color: #22C55E;
        }
        
        .alert-error {
            background: rgba(239, 68, 68, 0.2);
            border: 1px solid #EF4444;
            color: #EF4444;
        }
        
        .alert-info {
            background: rgba(59, 130, 246, 0.2);
            border: 1px solid #3B82F6;
            color: #3B82F6;
        }
        
        @media (max-width: 768px) {
            .container {
                padding: 15px;
            }
            
            .header h1 {
                font-size: 1.5rem;
            }
            
            .contract-preview {
                padding: 20px;
                max-height: 400px;
            }
            
            .signature-section {
                padding: 20px;
        }
        
        .signature-canvas {
                height: 150px;
            }
            
            .signature-methods {
                gap: 15px;
            }
            
            .signature-method {
                padding: 15px;
            }
            
            .actions {
                flex-direction: column;
            }
            
            .actions .btn {
            width: 100%;
            }
            
            .signature-typed-input {
                font-size: 1rem;
                padding: 12px;
            }
        }
        
        @media (max-width: 480px) {
            .container {
                padding: 10px;
            }
            
            .header h1 {
                font-size: 1.2rem;
            }
            
            .contract-preview {
                padding: 15px;
                max-height: 300px;
            }
            
            .signature-canvas {
                height: 120px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1><i class="fas fa-file-contract"></i> Assinar Contrato</h1>
            <p>Você foi convidado para assinar: <strong><%= contract.title %></strong></p>
            <% if (signer.signed_at) { %>
                <div class="alert alert-success">
                    <i class="fas fa-check-circle"></i> Este contrato já foi assinado em <%= new Date(signer.signed_at).toLocaleString('pt-BR') %>
                </div>
            <% } %>
        </div>
        
        <div class="contract-preview">
            <% if (contract.pdf_url && contract.contract_type === 'imported') { %>
            <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; flex-wrap: wrap; gap: 10px;">
                <h2 style="margin: 0;"><i class="fas fa-file-contract"></i> Visualização do Contrato</h2>
            </div>
            <!-- Apenas PDF (sem opção de texto) -->
            <div id="contract-pdf-view">
                <div style="background: #0B0B0B; border-radius: 8px; padding: 15px; margin-bottom: 15px;">
                    <div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 10px;">
                        <div style="display: flex; gap: 10px; align-items: center;">
                            <button id="pdf-prev-page" class="btn btn-secondary" style="padding: 6px 12px; font-size: 0.85rem;">
                                <i class="fas fa-chevron-left"></i>
                            </button>
                            <span id="pdf-page-info" style="color: #ECECEC; font-size: 0.9rem; min-width: 100px; text-align: center;">Página 1</span>
                            <button id="pdf-next-page" class="btn btn-secondary" style="padding: 6px 12px; font-size: 0.85rem;">
                                <i class="fas fa-chevron-right"></i>
                            </button>
                        </div>
                        <div style="display: flex; gap: 10px; align-items: center;">
                            <button id="pdf-zoom-out" class="btn btn-secondary" style="padding: 6px 12px; font-size: 0.85rem;">
                                <i class="fas fa-search-minus"></i>
                            </button>
                            <span id="pdf-zoom-level" style="color: #ECECEC; font-size: 0.9rem; min-width: 60px; text-align: center;">100%</span>
                            <button id="pdf-zoom-in" class="btn btn-secondary" style="padding: 6px 12px; font-size: 0.85rem;">
                                <i class="fas fa-search-plus"></i>
                            </button>
                        </div>
                    </div>
                </div>
                <div id="pdf-canvas-container" style="background: #2C2C2F; border-radius: 8px; padding: 20px; overflow: auto; max-height: 600px; text-align: center;">
                    <canvas id="pdf-canvas" style="max-width: 100%; height: auto; border-radius: 4px;"></canvas>
                </div>
            </div>
            <% } else { %>
            <!-- Contrato sem PDF (template): mostrar texto -->
            <h2 style="margin: 0 0 20px 0;"><i class="fas fa-file-alt"></i> Conteúdo do Contrato</h2>
            <div id="contract-text-view" class="contract-content"><%= (contract.pdf_content || contract.content || 'Conteúdo do contrato não disponível').replace(/\n/g, '<br>') %></div>
            <% } %>
                </div>
                
        <!-- Modal de visualização expandida -->
        <div id="contract-expanded-modal" style="display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.95); z-index: 10000; padding: 20px; overflow-y: auto;">
            <div style="max-width: 900px; margin: 0 auto; background: #1C1C21; border-radius: 16px; padding: 30px; position: relative;">
                <button id="close-expanded-btn" style="position: absolute; top: 20px; right: 20px; background: #2C2C2F; border: none; color: #FFFFFF; width: 40px; height: 40px; border-radius: 50%; cursor: pointer; font-size: 1.5rem; display: flex; align-items: center; justify-content: center;">
                    <i class="fas fa-times"></i>
                </button>
                <h2 style="color: #FFC700; margin-bottom: 20px;"><i class="fas fa-file-alt"></i> <%= contract.title %></h2>
                <div style="background: #0B0B0B; padding: 30px; border-radius: 12px; white-space: pre-wrap; line-height: 1.8; color: #ECECEC; max-height: 70vh; overflow-y: auto;" id="expanded-contract-content"><%= (contract.pdf_content || contract.content || 'Conteúdo do contrato não disponível').replace(/\n/g, '<br>') %></div>
            </div>
                </div>
                
        <% if (signer.signed_at) { %>
        <div class="signature-section" style="background: rgba(34, 197, 94, 0.1); border: 2px solid #22C55E; padding: 30px; border-radius: 12px; text-align: center;">
            <div style="font-size: 4rem; margin-bottom: 20px; color: #22C55E;">
                <i class="fas fa-check-circle"></i>
            </div>
            <h3 style="color: #22C55E; margin-bottom: 20px;">Contrato Assinado com Sucesso!</h3>
            <p style="color: #ECECEC; margin-bottom: 15px; font-size: 1.1rem;">
                Você assinou este contrato em <strong><%= new Date(signer.signed_at).toLocaleString('pt-BR') %></strong>.
            </p>
            <p style="color: #888888; font-size: 0.9rem; margin-bottom: 30px;">
                Sua assinatura foi registrada com sucesso e tem validade legal.
            </p>
            
            <% 
            // Buscar status do contrato para verificar se está completo
            const contractStatus = contract.status || 'sent';
            const isCompleted = contractStatus === 'completed';
            %>
            
            <% if (isCompleted) { %>
            <div style="background: rgba(255, 199, 0, 0.1); border: 2px solid #FFC700; border-radius: 8px; padding: 20px; margin-bottom: 20px;">
                <p style="color: #FFC700; margin: 0; font-weight: 600;">
                    <i class="fas fa-check-double"></i> Todos os signatários assinaram! O contrato está completo.
                </p>
            </div>
            <% } %>
            
            <div style="display: flex; gap: 15px; justify-content: center; flex-wrap: wrap;">
                <a href="/contract/sign/<%= signToken %>/download" 
                   class="btn btn-primary" 
                   style="min-width: 200px; text-decoration: none; display: inline-flex; align-items: center; gap: 8px;">
                    <i class="fas fa-download"></i> Baixar Contrato Assinado
                </a>
            </div>
        </div>
        <% } else { %>
        <div class="signature-section" id="signature-section">
            <h3><i class="fas fa-signature"></i> Assinatura Eletrônica</h3>
            
            <div class="signature-methods">
                <!-- Método 1: Desenhar no Canvas -->
                <div class="signature-method active" id="canvas-method">
                    <h4><i class="fas fa-pencil-alt"></i> Assinar Desenhando</h4>
                    <p style="color: #888888; font-size: 0.9rem; margin-bottom: 15px;">
                        <i class="fas fa-info-circle"></i> Desenhe sua assinatura usando o mouse ou o dedo (em dispositivos touch)
                    </p>
                    <div class="signature-canvas-container">
                        <canvas id="signature-canvas" class="signature-canvas"></canvas>
                    </div>
                    <div class="canvas-controls" style="display: flex; gap: 10px; margin-top: 15px; align-items: center;">
                        <button type="button" class="btn btn-secondary" id="clear-canvas">
                            <i class="fas fa-undo"></i> Limpar
                        </button>
                        <div id="signature-validation" style="flex: 1; padding: 10px; background: rgba(34, 197, 94, 0.1); border-radius: 8px; display: none;">
                            <small style="color: #22C55E;">
                                <i class="fas fa-check-circle"></i> Assinatura detectada
                            </small>
                        </div>
                    </div>
                </div>
                
                <!-- Método 2: Upload de Imagem -->
                <div class="signature-method" id="upload-method">
                    <h4><i class="fas fa-upload"></i> Enviar Imagem da Assinatura</h4>
                    <p style="color: #888888; font-size: 0.9rem; margin-bottom: 15px;">
                        <i class="fas fa-info-circle"></i> Envie uma foto ou imagem da sua assinatura (PNG, JPG - máx. 5MB)
                    </p>
                    <div class="signature-upload-container">
                        <input type="file" id="signature-upload" accept="image/png,image/jpeg,image/jpg" class="form-control">
                        <div id="signature-upload-preview" style="margin-top: 15px; display: none;">
                            <img id="uploaded-signature" style="max-width: 100%; max-height: 200px; border-radius: 8px; border: 2px solid #FFC700;">
                            <div id="image-validation" style="margin-top: 10px; padding: 10px; background: rgba(34, 197, 94, 0.1); border-radius: 8px; display: none;">
                                <small style="color: #22C55E;">
                                    <i class="fas fa-check-circle"></i> Imagem válida detectada
                                </small>
                            </div>
                        </div>
                    </div>
                </div>
                
                <!-- Método 3: Digitar Nome -->
                <div class="signature-method" id="typed-method">
                    <h4><i class="fas fa-keyboard"></i> Digitar Nome</h4>
                    <p style="color: #888888; font-size: 0.9rem; margin-bottom: 15px;">
                        <i class="fas fa-info-circle"></i> Digite seu nome completo como assinatura
                    </p>
                    <input type="text" id="signature-typed" class="signature-typed-input" placeholder="Digite seu nome completo">
                    <div id="typed-validation" style="margin-top: 10px; padding: 10px; background: rgba(34, 197, 94, 0.1); border-radius: 8px; display: none;">
                        <small style="color: #22C55E;">
                            <i class="fas fa-check-circle"></i> Nome válido
                        </small>
                    </div>
                    </div>
                </div>
            
            <div class="checkbox-group">
                <input type="checkbox" id="accept-terms" required>
                <label for="accept-terms">
                    Eu aceito e concordo com os termos deste contrato. Esta assinatura tem validade legal.
                </label>
            </div>
            
            <div class="actions">
                <button type="button" class="btn btn-secondary" id="cancel-btn">
                    <i class="fas fa-times"></i> Cancelar
                </button>
                <button type="button" class="btn btn-primary" id="submit-signature-btn" style="min-width: 200px;">
                    <i class="fas fa-check-circle"></i> Confirmar e Assinar
                    </button>
            </div>
            
            <div style="margin-top: 20px; padding: 15px; background: rgba(255, 199, 0, 0.1); border-left: 4px solid #FFC700; border-radius: 6px;">
                <p style="color: #ECECEC; font-size: 0.9rem; margin: 0;">
                    <i class="fas fa-shield-alt" style="color: #FFC700;"></i> 
                    <strong>Segurança:</strong> Sua assinatura será registrada com data, hora e informações de segurança. Esta ação é irreversível.
                </p>
                    </div>
                </div>
                <% } %>
    </div>
    
    <script>
        const signToken = '<%= signToken %>';
        const contractId = <%= contract.id %>;
        let signaturePad = null;
        let currentSignatureMethod = 'canvas';
        let uploadedSignatureData = null;
        
        // Inicializar Signature Pad (canvas)
        document.addEventListener('DOMContentLoaded', function() {
            // Verificar se pdfjsLib está disponível antes de usar
            if (typeof pdfjsLib === 'undefined') {
                console.warn('PDF.js não carregado. Tentando recarregar...');
                // Tentar recarregar PDF.js
                const script = document.createElement('script');
                script.src = 'https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.min.js';
                script.onload = function() {
                    if (typeof pdfjsLib !== 'undefined') {
                        pdfjsLib.GlobalWorkerOptions.workerSrc = 'https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js';
                        console.log('PDF.js carregado com sucesso');
                    }
                };
                document.head.appendChild(script);
            }
            
            // Carregar PDF automaticamente (apenas visualização em PDF)
            <% if (contract.pdf_url && contract.contract_type === 'imported') { %>
            let pdfDoc = null;
            let currentPdfPage = 1;
            let pdfScale = 1.0;
            
            async function loadPdf() {
                try {
                    if (typeof pdfjsLib === 'undefined') {
                        throw new Error('PDF.js não está carregado. Aguarde alguns segundos e recarregue a página.');
                    }
                    if (!pdfjsLib.GlobalWorkerOptions.workerSrc) {
                        pdfjsLib.GlobalWorkerOptions.workerSrc = 'https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js';
                    }
                    const pdfUrl = '/contract/sign/<%= signToken %>/pdf';
                    const response = await fetch(pdfUrl);
                    if (!response.ok) throw new Error('Não foi possível carregar o PDF.');
                    const blob = await response.blob();
                    const arrayBuffer = await blob.arrayBuffer();
                    const loadingTask = pdfjsLib.getDocument(arrayBuffer);
                    pdfDoc = await loadingTask.promise;
                    if (!pdfDoc || pdfDoc.numPages === 0) {
                        throw new Error('O documento não possui páginas.');
                    }
                    currentPdfPage = 1;
                    await renderPdfPage();
                    updatePdfControls();
                } catch (error) {
                    console.error('Erro ao carregar PDF:', error);
                    const container = document.getElementById('pdf-canvas-container');
                    if (container) {
                        container.innerHTML = '<p style="color: #ef4444; padding: 20px;">Erro ao carregar o PDF. ' + (error.message || 'Tente recarregar a página.') + '</p>';
                    }
                }
            }
            
            loadPdf();
            
            // Função para renderizar página do PDF
            async function renderPdfPage() {
                if (!pdfDoc) return;
                const canvas = document.getElementById('pdf-canvas');
                const loadingDiv = document.getElementById('pdf-loading');
                const errorDiv = document.getElementById('pdf-error');
                
                if (!canvas) return;
                const pageNum = Math.max(1, Math.min(currentPdfPage, pdfDoc.numPages));
                currentPdfPage = pageNum;
                
                try {
                    // Mostrar loading
                    if (loadingDiv) loadingDiv.style.display = 'block';
                    if (errorDiv) errorDiv.style.display = 'none';
                    canvas.style.display = 'none';
                    
                    const page = await pdfDoc.getPage(currentPdfPage - 1); // PDF.js usa índice 0-based
                    const viewport = page.getViewport({ scale: pdfScale });
                    
                    canvas.height = viewport.height;
                    canvas.width = viewport.width;
                    
                    const context = canvas.getContext('2d');
                    context.clearRect(0, 0, canvas.width, canvas.height);
                    await page.render({ canvasContext: context, viewport: viewport }).promise;
                    
                    // Esconder loading e mostrar canvas
                    if (loadingDiv) loadingDiv.style.display = 'none';
                    canvas.style.display = 'block';
                } catch (error) {
                    console.error('Erro ao renderizar PDF:', error);
                    if (loadingDiv) loadingDiv.style.display = 'none';
                    if (errorDiv) errorDiv.style.display = 'block';
                    canvas.style.display = 'none';
                }
            }
            
            // Função para atualizar controles do PDF
            function updatePdfControls() {
                const pageInfo = document.getElementById('pdf-page-info');
                const zoomLevel = document.getElementById('pdf-zoom-level');
                
                if (pageInfo && pdfDoc) {
                    pageInfo.textContent = `Página ${currentPdfPage} de ${pdfDoc.numPages}`;
                }
                
                if (zoomLevel) {
                    zoomLevel.textContent = Math.round(pdfScale * 100) + '%';
                }
            }
            
            // Controles de navegação do PDF
            document.getElementById('pdf-prev-page')?.addEventListener('click', function() {
                if (currentPdfPage > 1) {
                    currentPdfPage--;
                    renderPdfPage();
                    updatePdfControls();
                }
            });
            
            document.getElementById('pdf-next-page')?.addEventListener('click', function() {
                if (pdfDoc && currentPdfPage < pdfDoc.numPages) {
                    currentPdfPage++;
                    renderPdfPage();
                    updatePdfControls();
                }
            });
            
            document.getElementById('pdf-zoom-in')?.addEventListener('click', function() {
                pdfScale = Math.min(pdfScale + 0.25, 3.0);
                renderPdfPage();
                updatePdfControls();
            });
            
            document.getElementById('pdf-zoom-out')?.addEventListener('click', function() {
                pdfScale = Math.max(pdfScale - 0.25, 0.5);
                renderPdfPage();
                updatePdfControls();
            });
            <% } %>
            const canvas = document.getElementById('signature-canvas');
            if (canvas) {
                signaturePad = new SignaturePad(canvas);
                
                // Ajustar canvas para retina
                const ratio = Math.max(window.devicePixelRatio || 1, 1);
                canvas.width = canvas.offsetWidth * ratio;
                canvas.height = canvas.offsetHeight * ratio;
                canvas.getContext('2d').scale(ratio, ratio);
                signaturePad.clear();
            }
            
            // Registrar acesso
            fetch(`/contract/sign/${signToken}/start`, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                }
            }).catch(err => console.error('Erro ao registrar acesso:', err));
            
            // Limpar canvas
            document.getElementById('clear-canvas')?.addEventListener('click', function() {
                if (signaturePad) {
                    signaturePad.clear();
                    document.getElementById('signature-validation').style.display = 'none';
                }
            });
            
            // Validação de assinatura no canvas (detectar se há traços)
            if (signaturePad) {
                signaturePad.addEventListener('endStroke', function() {
                    if (!signaturePad.isEmpty()) {
                        // Validar se tem traços suficientes (mínimo de complexidade)
                        const data = signaturePad.toData();
                        if (data.length > 3) { // Pelo menos 3 pontos de traço
                            document.getElementById('signature-validation').style.display = 'block';
                        }
                    }
                });
            }
            
            // Upload de imagem com validação
            document.getElementById('signature-upload')?.addEventListener('change', function(e) {
                const file = e.target.files[0];
                if (file) {
                    // Validar tamanho (máximo 5MB)
                    if (file.size > 5 * 1024 * 1024) {
                        alert('⚠️ A imagem é muito grande. Por favor, use uma imagem menor que 5MB.');
                        this.value = '';
                        return;
                    }
                    
                    // Validar tipo
                    if (!file.type.match(/^image\/(png|jpeg|jpg)$/)) {
                        alert('⚠️ Por favor, selecione uma imagem válida (PNG ou JPG).');
                        this.value = '';
                        return;
                    }
                    
                const reader = new FileReader();
                    reader.onload = function(event) {
                        uploadedSignatureData = event.target.result;
                        document.getElementById('signature-upload-preview').style.display = 'block';
                        document.getElementById('uploaded-signature').src = event.target.result;
                        
                        // Validar se parece uma assinatura (imagem não muito simples)
                        const img = new Image();
                        img.onload = function() {
                            // Verificar dimensões mínimas
                            if (img.width >= 100 && img.height >= 50) {
                                document.getElementById('image-validation').style.display = 'block';
                            }
                        };
                        img.src = event.target.result;
                        
                        currentSignatureMethod = 'upload';
                };
                reader.readAsDataURL(file);
            }
            });
            
            // Digitar nome com validação
            document.getElementById('signature-typed')?.addEventListener('input', function() {
                const value = this.value.trim();
                if (value.length >= 3) { // Mínimo 3 caracteres
                    currentSignatureMethod = 'typed';
                    // Ativar método digitado visualmente
                    document.querySelectorAll('.signature-method').forEach(m => m.classList.remove('active'));
                    document.getElementById('typed-method').classList.add('active');
                    document.getElementById('typed-validation').style.display = 'block';
                } else {
                    document.getElementById('typed-validation').style.display = 'none';
                }
            });
            
            // Ativar método canvas quando desenhar
            if (signaturePad) {
                signaturePad.addEventListener('beginStroke', function() {
                    document.querySelectorAll('.signature-method').forEach(m => m.classList.remove('active'));
                    document.getElementById('canvas-method').classList.add('active');
                    currentSignatureMethod = 'canvas';
                });
            }
            
            // Trocar método de assinatura
            document.querySelectorAll('.signature-method').forEach(method => {
                method.addEventListener('click', function(e) {
                    if (e.target.tagName === 'BUTTON' || e.target.tagName === 'INPUT') return;
                    document.querySelectorAll('.signature-method').forEach(m => m.classList.remove('active'));
                    this.classList.add('active');
                    
                    if (this.id === 'canvas-method') currentSignatureMethod = 'canvas';
                    else if (this.id === 'upload-method') currentSignatureMethod = 'upload';
                    else if (this.id === 'typed-method') currentSignatureMethod = 'typed';
                });
            });
            
            // Submeter assinatura
            document.getElementById('submit-signature-btn')?.addEventListener('click', async function() {
                const acceptTerms = document.getElementById('accept-terms');
                if (!acceptTerms.checked) {
                    alert('Você deve aceitar os termos do contrato para continuar.');
                return;
            }
            
                let signatureData = null;
                let signatureImageUrl = null;
                
                if (currentSignatureMethod === 'canvas') {
                    if (signaturePad.isEmpty()) {
                        alert('⚠️ Por favor, desenhe sua assinatura no espaço acima antes de continuar.');
                        document.getElementById('canvas-method').scrollIntoView({ behavior: 'smooth', block: 'center' });
                        return;
                    }
                    signatureData = signaturePad.toDataURL('image/png');
                    signatureImageUrl = signatureData;
                } else if (currentSignatureMethod === 'upload') {
                    if (!uploadedSignatureData) {
                        alert('⚠️ Por favor, selecione uma imagem da sua assinatura antes de continuar.');
                        document.getElementById('upload-method').scrollIntoView({ behavior: 'smooth', block: 'center' });
                        return;
                    }
                    signatureData = uploadedSignatureData;
                    signatureImageUrl = uploadedSignatureData;
                } else if (currentSignatureMethod === 'typed') {
                    const typedName = document.getElementById('signature-typed').value.trim();
                    if (!typedName) {
                        alert('⚠️ Por favor, digite seu nome completo antes de continuar.');
                        document.getElementById('typed-method').scrollIntoView({ behavior: 'smooth', block: 'center' });
                return;
            }
                    signatureData = typedName;
                }
                
                this.disabled = true;
                this.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Processando...';
            
            try {
                    const response = await fetch(`/contract/sign/${signToken}/submit`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                        body: JSON.stringify({
                            signature_type: currentSignatureMethod,
                            signature_data: signatureData,
                            signature_image_url: signatureImageUrl
                        })
                });
                
                const result = await response.json();
                
                    if (result.success) {
                        // Mostrar mensagem de sucesso melhorada
                        const successMessage = result.data?.completed 
                            ? '🎉 Assinatura realizada com sucesso! Todos os signatários assinaram. O contrato está completo!'
                            : '✅ Assinatura realizada com sucesso!';
                        
                        // Criar modal de sucesso premium
                        const modal = document.createElement('div');
                        modal.style.cssText = `
                            position: fixed;
                            top: 0;
                            left: 0;
                            width: 100%;
                            height: 100%;
                            background: rgba(0, 0, 0, 0.8);
                            z-index: 10000;
                            display: flex;
                            align-items: center;
                            justify-content: center;
                            animation: fadeIn 0.3s ease;
                        `;
                        modal.innerHTML = `
                            <div style="background: linear-gradient(135deg, #1C1C21 0%, #2C2C2F 100%); 
                                        border-radius: 16px; 
                                        padding: 40px; 
                                        max-width: 500px; 
                                        text-align: center;
                                        box-shadow: 0 20px 60px rgba(0,0,0,0.5);
                                        border: 2px solid #FFC700;">
                                <div style="font-size: 5rem; color: #22C55E; margin-bottom: 20px;">
                            <i class="fas fa-check-circle"></i>
                            </div>
                                <h2 style="color: #FFFFFF; margin-bottom: 20px; font-size: 1.8rem;">
                                    ${result.data?.completed ? '🎉 Contrato Completo!' : '✅ Assinatura Confirmada!'}
                                </h2>
                                <p style="color: #ECECEC; margin-bottom: 30px; font-size: 1.1rem; line-height: 1.6;">
                                    ${successMessage}
                                </p>
                                ${result.data?.downloadUrl ? `
                                    <a href="${result.data.downloadUrl}" 
                                       class="btn btn-primary" 
                                       style="min-width: 250px; text-decoration: none; display: inline-flex; align-items: center; gap: 10px; margin-bottom: 15px;">
                                        <i class="fas fa-download"></i> Baixar Contrato Assinado
                                    </a>
                                ` : ''}
                                <button onclick="window.location.reload()" 
                                        class="btn btn-secondary" 
                                        style="min-width: 250px;">
                                    Ver Contrato
                                </button>
                        </div>
                    `;
                        document.body.appendChild(modal);
                        
                        // Recarregar após 3 segundos se não clicar
                        setTimeout(() => {
                            if (modal.parentElement) {
                                window.location.reload();
                            }
                        }, 5000);
                } else {
                        alert('Erro ao assinar: ' + (result.error?.message || 'Erro desconhecido'));
                        this.disabled = false;
                        this.innerHTML = '<i class="fas fa-check-circle"></i> Confirmar e Assinar';
                }
            } catch (error) {
                    alert('Erro ao assinar: ' + error.message);
                    this.disabled = false;
                    this.innerHTML = '<i class="fas fa-check"></i> Confirmar Assinatura';
                }
            });
        });
    </script>
</body>
</html>
