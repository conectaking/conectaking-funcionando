# 🚀 Comandos para Fazer Push no Bitbucket

## ✅ Repositório já conectado!

O repositório já está configurado para o Bitbucket:
https://conectaking@bitbucket.org/conecta-king-backend/conecta-king-backend.git

---

## 📤 Fazer Push das Alterações

### Opção 1: Push Normal (se já houver commits no Bitbucket)

```bash
cd "C:\Users\playa\Desktop\CONECTA KING MVP DEZEMBRO\backend-conecta-king"
git push -u origin main
```

### Opção 2: Se a branch for "master"

```bash
git push -u origin master
```

### Opção 3: Force Push (use apenas se necessário e souber o que está fazendo)

```bash
git push -u origin main --force
```

---

## 🔐 Autenticação

O Bitbucket vai pedir suas credenciais:
- **Usuário:** conectaking
- **Senha:** Use uma App Password (não sua senha normal)

**Como criar App Password:**
1. Acesse: https://bitbucket.org/account/settings/app-passwords/
2. Clique em "Create app password"
3. Dê um nome (ex: "Render Deploy")
4. Selecione permissões: "Repositories: Write"
5. Copie a senha gerada
6. Use essa senha quando o Git pedir

---

## ⚠️ Se Der Erro

### Erro: "Permission denied"
- Verifique se você tem permissão no repositório
- Use App Password ao invés de senha normal

### Erro: "Branch diverged"
- Tente: `git pull origin main --rebase` primeiro
- Depois: `git push origin main`

### Erro: "Repository not found"
- Verifique se a URL está correta
- Verifique se você tem acesso ao repositório

---

## ✅ Depois do Push

1. O Render (se configurado) fará deploy automaticamente
2. Ou faça deploy manual no painel do Render
3. Configure as variáveis SMTP no Render:
   - SMTP_HOST=smtp.gmail.com
   - SMTP_PORT=587
   - SMTP_SECURE=false
   - SMTP_USER=conectaking@gmail.com
   - SMTP_PASS=imhr ogpa zeqg scms
   - SMTP_FROM=noreply@conectaking.com.br

---

## 🧪 Testar

Após o deploy no Render, teste:
- https://conectaking-api.onrender.com/api/health
- Página de recuperação de senha

