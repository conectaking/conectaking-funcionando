# ===========================================
# AWS Rekognition + S3 Staging (reconhecimento facial KingSelection)
# Adicione estas variáveis ao seu .env (não commitar .env)
# ===========================================

# AWS (us-east-1 para Rekognition)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Rekognition
REKOG_COLLECTION_ID=kingselection
REKOG_FACE_MATCH_THRESHOLD=85
REKOG_MAX_FACES_PER_IMAGE=10

# S3 Staging (ponte temporária para o Rekognition)
S3_STAGING_BUCKET=kingselection-rekog-staging
S3_STAGING_PREFIX=staging/

# Cache (evitar reprocessar mesma foto)
CACHE_TTL_SECONDS=2592000

# SQS (quando implementar fila + worker)
# SQS_QUEUE_URL=
# SQS_DLQ_URL=
# WORKER_CONCURRENCY=10

# R2 já configurado no .env principal (R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET, R2_PUBLIC_BASE_URL)
